# DESIGN.md — alisherotabaev.com

Дизайн-система сайта. Формат — [Stitch DESIGN.md](https://stitch.withgithub.com/docs/design-md/specification/),
чтобы ИИ-агент читал её перед вёрсткой и не изобретал стиль заново.

Все значения взяты из работающего кода (`_tools/shared/layout.mjs`), а не
придуманы. Контрасты посчитаны, не на глаз.

---

## 1. Настроение и характер

Спокойный деловой минимализм с тёплым акцентом. Тёмно-синий вместо чёрного,
приглушённое золото вместо яркого акцента, кремовый фон вместо серого.

Ощущение, к которому стремимся: **человек, который давно в деле, а не
стартап с лендингом**. Отсюда — мало цвета, много воздуха, крупная
типографика и никаких градиентных кнопок с неоновым свечением.

Содержание всегда важнее оформления. Если элемент не помогает понять или
дойти до цели — его не должно быть.

---

## 2. Палитра и роли

| Переменная | Значение | Где применяется |
|---|---|---|
| `--ink` | `#000d29` | основной текст, тёмные плашки, заголовки |
| `--ink-soft` | `#26324a` | текст внутри карточек, второстепенные подписи |
| `--muted` | `#5c6472` | пояснения, даты, счётчики |
| `--gold` | `#8f7151` | акцент: рамки при наведении, декоративные детали |
| `--gold-dk` | `#876638` | акцентный текст и ссылки — тёмный вариант |
| `--paper` | `#ffffff` | фон страницы и карточек |
| `--cream` | `#faf8f4` | фон боковой панели и чередующихся секций |
| `--cream2` | `#f3ede3` | фон меток и наведения на пункты меню |
| `--line` | `#e8e1d6` | рамки, разделители |

### Контрасты — проверено

| Пара | Контраст | Годность |
|---|---|---|
| `--ink` на `--paper` | 19.27 | да |
| `--ink-soft` на `--paper` | 12.82 | да |
| `--muted` на `--paper` | 5.96 | да |
| `--muted` на `--cream` | 5.62 | да |
| `--gold-dk` на `--paper` | 5.27 | да |
| `--gold-dk` на `--cream` | 4.96 | да |
| белый на `--ink` | 19.27 | да |
| белый на `--gold` | 4.52 | впритык, но проходит |
| **белый на `#29a9eb`** | **2.64** | **не проходит** |

**Два предупреждения, которые нельзя потерять.**

Золото уже затемняли: было `#b89168`, белый текст на нём давал 2.88 при
норме 4.5. Не возвращайте прежний оттенок для текстовых поверхностей —
в декоративных градиентах, где текста нет, он остался.

Фирменный синий Telegram `#29a9eb` с белым текстом даёт 2.64. Кнопка
`.btn-tg` этим цветом пользуется — это осознанный компромисс ради
узнаваемости бренда. Не тиражируйте его на другие элементы.

---

## 3. Типографика

Два начертания, оба со своего домена, вариативные:

- **Roboto Condensed** — все заголовки `h1`–`h4` и класс `.cond`;
- **Roboto** — весь остальной текст.

Обращений к `fonts.googleapis.com` быть не должно ни одного: шрифты
самостоятельные, с `unicode-range` под кириллицу и латиницу отдельно.

### Шкала

| Роль | Размер | Начертание | Междустрочный |
|---|---|---|---|
| Заголовок главной | 52px | 700 | 1.06 |
| Заголовок раздела | 42px | 700 | — |
| Заголовок секции | 40px | 700 | 1.1 |
| Крупный подзаголовок | 30px | 700 | — |
| Заголовок блока | 27px | 700 | — |
| Заголовок карточки | 22px | 700 | 1.3 |
| Вопрос в FAQ | 21px | 700 | — |
| Вводный абзац | 20px | 400 | — |
| Основной текст | 17px | 400 | 1.6 |
| Текст в карточке | 15px | 400 | 1.55 |
| Метка, счётчик | 11.5–14px | 500–600 | — |

Межбуквенное расстояние: у крупных заголовков `-.01em`, у меток капсом
`.03em`–`.14em`.

Заголовки набираются обычным регистром. Капс — только у меток и надписей
на кнопках-таблетках.

---

## 4. Компоненты

### Кнопки

Все — таблетки, `border-radius:999px`, `padding:15px 26px`, вес 600,
размер 16px. При наведении приподнимаются на 2 пикселя.

- `.btn-primary` — фон `--ink`, белый текст, при наведении `--gold-dk`;
- `.btn-ghost` — белый фон, рамка `1.5px solid --line`, при наведении
  рамка становится `--gold`;
- `.btn-tg` — `#29a9eb` (см. предупреждение о контрасте);
- `.btn-max` — фон `--gold`.

### Карточки

Белый фон, рамка `1px solid --line`, скругление 14px, отступы `22px`.
При наведении приподнимаются на 3 пикселя, получают `--shadow` и золотую
рамку. Внутри: метка капсом, заголовок 22px, описание 15px, ссылка внизу.

Сетка: `repeat(auto-fill, minmax(17rem, 1fr))`, промежуток `1rem`.

### Метки и фильтры

`.chip` — таблетка с рамкой, 13px, вес 500. Выбранная получает
`aria-pressed="true"`, тёмный фон и белый текст. Состояние обязательно
передаётся атрибутом, а не только цветом.

### Боковое меню

От 1200px — фиксированная панель 248px слева, фон `--cream`, правая
граница `--line`. Ниже 1200px её нет вовсе, работает верхняя шапка.
Переключение — медиавыражением, без скриптов.

---

## 5. Раскладка и отступы

- Контейнер `.wrap` — `max-width:1180px`, боковые отступы 24px.
- Секции — `padding:70px 0` **глобально для тега `section`**.
- Промежутки в сетках: 10, 14, 18, 22, 26px.
- Колонка чтения — не шире 760px, длинная строка утомляет.

**Ловушка, на которой уже спотыкались.** Правило `section{padding:70px 0}`
применяется ко всем `<section>` без разбора. Новый блок, свёрстанный
секциями, получит по 140 пикселей пустоты между группами. В таких случаях
гасите явно: `padding:0`.

---

## 6. Глубина

Две тени, больше не нужно:

- `--shadow-sm` — `0 4px 18px rgba(0,13,41,.06)` — кнопки в покое;
- `--shadow` — `0 10px 40px rgba(0,13,41,.08)` — карточки при наведении.

Тени мягкие и в синеву, не в серость: цвет взят от `--ink`, а не от чёрного.
Никаких резких теней и обводок. Глубина создаётся рамками и фоном, тень —
только отклик на действие.

---

## 7. Что делать и чего не делать

### Делать

- Отрисовывать содержимое **на этапе сборки**. Краулеры ИИ-ассистентов —
  GPTBot, ClaudeBot, PerplexityBot — не выполняют JavaScript. Именно с этой
  проблемы начался проект: каталог собирался в браузере, и для них страница
  была пустой.
- Скрипты — только для необязательного: фильтр, поиск по уже отрисованному
  списку. Без них страница обязана оставаться полной.
- Разбор по группам делать якорями `#id`, а не вкладками на скриптах.
- Состояние элемента передавать атрибутом (`aria-pressed`, `hidden`),
  а не одним лишь цветом.
- Проверять контраст расчётом. Норма — 4.5 для обычного текста.

### Не делать

- Не подключать шрифты с чужих доменов.
- Не делать раскладку скриптами: только CSS.
- Не вводить третий шрифт и четвёртый оттенок акцента.
- Не ставить тени плотнее `.08` — вид становится дешёвым.
- Не растягивать текстовую колонку на всю ширину экрана.
- Не возвращать `#b89168` под белый текст.

---

## 8. Поведение при разной ширине

Точки перелома, которые уже используются:

| Ширина | Что меняется |
|---|---|
| от 1200px | появляется боковое меню, шапка прячется, `body` сдвигается на 248px |
| до 980px | сетка `.grid` перестраивается |
| до 900px | `.cards3` из трёх колонок схлопывается |
| до 860px | часть пунктов верхнего меню (`.hideable`) скрывается |
| до 720px | уменьшаются заголовки главной, секций и призыва; метки уезжают в горизонтальную прокрутку |
| до 640px | `.grid` схлопывается окончательно |

Кликаемые элементы — не меньше 44 пикселей по высоте. Списки меток на
узких экранах прокручиваются вбок, край подсвечивается градиентом.

---

## 9. Подсказка для агента

Короткая выжимка, чтобы вставить в запрос:

```
Палитра: текст #000d29, второстепенный #5c6472, акцент #876638,
фон #ffffff, тёплый фон #faf8f4, рамки #e8e1d6.
Шрифты: заголовки Roboto Condensed 700, текст Roboto 400 17px/1.6.
Кнопки — таблетки 999px, карточки — скругление 14px, рамка 1px #e8e1d6.
Тень только при наведении: 0 10px 40px rgba(0,13,41,.08).
Контейнер 1180px, отступы секций 70px сверху и снизу.
Всё содержимое — в HTML на этапе сборки, скрипты только для фильтров.
Контраст текста не ниже 4.5.
```
